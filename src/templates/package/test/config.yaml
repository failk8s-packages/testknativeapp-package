#@ load("@ytt:data", "data")

---
apiVersion: v1
kind: Secret
metadata:
  name: #@ "{}-config".format(data.values.package.name)
  namespace: #@ "{}-package".format(data.values.package.name)
stringData:
  values.yml: 